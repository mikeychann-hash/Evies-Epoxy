# ============================================================================
# EVIE'S EPOXY - Environment Configuration
# ============================================================================
#
# This file contains all required environment variables for the application.
# Copy this file to .env and fill in your actual values.
#
# SECURITY NOTE: Never commit .env to version control!
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
#
# Local development (with Docker Compose):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/evies_epoxy"
#
# Production examples:
# - Vercel Postgres: postgresql://[user]:[pass]@[id].us-east-1.postgres.vercel-storage.com/[db]
# - Neon: postgresql://[user]:[pass]@[host]/[db]?sslmode=require
# - AWS RDS: postgresql://[user]:[pass]@[endpoint].rds.amazonaws.com:5432/[db]
# - DigitalOcean: postgresql://[user]:[pass]@[cluster]-do-user-[id].db.ondigitalocean.com:25060/[db]?sslmode=require

# ============================================================================
# AUTHENTICATION (NextAuth.js)
# ============================================================================

# Application URL - MUST match your deployment domain
# Development:
NEXTAUTH_URL="http://localhost:3000"
# Production:
# NEXTAUTH_URL="https://yourdomain.com"

# Secret for JWT signing and encryption
# Generate with: openssl rand -base64 32
# CRITICAL: Keep this secret! Never expose publicly!
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
#
# Example generation:
# $ openssl rand -base64 32
# dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIGV4YW1wbGU=

# ============================================================================
# STRIPE PAYMENT PROCESSING
# ============================================================================

# Stripe Publishable Key (client-side, safe to expose)
# Get from: Stripe Dashboard → Developers → API Keys
# Development (test mode):
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_key_here"
# Production (live mode):
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_your_key_here"

# Stripe Secret Key (server-side, KEEP PRIVATE!)
# Development (test mode):
STRIPE_SECRET_KEY="sk_test_your_key_here"
# Production (live mode):
# STRIPE_SECRET_KEY="sk_live_your_key_here"

# Stripe Webhook Signing Secret
# Get from: Stripe Dashboard → Developers → Webhooks → Your endpoint
# Required for webhook signature verification (security)
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"
#
# Webhook URL to configure in Stripe Dashboard:
# - Development: Use Stripe CLI: stripe listen --forward-to localhost:3000/api/webhooks/stripe
# - Production: https://yourdomain.com/api/webhooks/stripe
#
# Required events:
# - checkout.session.completed
# - payment_intent.succeeded
# - payment_intent.payment_failed

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Public application URL (used for redirects, SEO, emails)
# Development:
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Production:
# NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# Application display name (shown in UI, emails, meta tags)
NEXT_PUBLIC_APP_NAME="Evie's Epoxy"

# ============================================================================
# ADMIN CONFIGURATION
# ============================================================================

# Email address for first admin user
# The FIRST user to register with this email will become ADMIN
# All subsequent users will be regular USER role
ADMIN_EMAIL="admin@example.com"

# ============================================================================
# OPTIONAL: EMAIL SERVICE (for order confirmations, notifications)
# ============================================================================

# Uncomment and configure if using Resend (recommended)
# RESEND_API_KEY="re_your_api_key_here"
# RESEND_FROM_EMAIL="orders@yourdomain.com"

# Uncomment and configure if using SendGrid
# SENDGRID_API_KEY="SG.your_api_key_here"
# SENDGRID_FROM_EMAIL="orders@yourdomain.com"

# Uncomment and configure if using Nodemailer (SMTP)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# SMTP_FROM="orders@yourdomain.com"

# ============================================================================
# OPTIONAL: CLOUD STORAGE (for product images)
# ============================================================================

# Uncomment and configure if using AWS S3
# AWS_ACCESS_KEY_ID="your_access_key"
# AWS_SECRET_ACCESS_KEY="your_secret_key"
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="evies-epoxy-images"

# Uncomment and configure if using Cloudinary
# CLOUDINARY_CLOUD_NAME="your_cloud_name"
# CLOUDINARY_API_KEY="your_api_key"
# CLOUDINARY_API_SECRET="your_api_secret"

# ============================================================================
# OPTIONAL: ANALYTICS & MONITORING
# ============================================================================

# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# Plausible Analytics (privacy-friendly alternative)
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN="yourdomain.com"

# Sentry (error tracking)
# SENTRY_DSN="https://[key]@[org].ingest.sentry.io/[project]"
# SENTRY_AUTH_TOKEN="your_auth_token"

# ============================================================================
# DEVELOPMENT ONLY (do not use in production)
# ============================================================================

# Node environment (automatically set by most platforms)
NODE_ENV="development"
# Production will use: NODE_ENV="production"

# Enable debug logging (NextAuth)
# NEXTAUTH_DEBUG="true"

# ============================================================================
# QUICK SETUP CHECKLIST
# ============================================================================
#
# 1. [ ] Copy this file: cp .env.example .env
# 2. [ ] Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 3. [ ] Setup PostgreSQL database (Docker or cloud provider)
# 4. [ ] Get Stripe API keys from dashboard
# 5. [ ] Configure Stripe webhook endpoint
# 6. [ ] Update NEXTAUTH_URL and NEXT_PUBLIC_APP_URL
# 7. [ ] Set ADMIN_EMAIL for first admin user
# 8. [ ] Run: npm install
# 9. [ ] Run: npm run prisma:generate
# 10. [ ] Run: npm run prisma:push (creates tables)
# 11. [ ] Run: npm run dev
#
# ============================================================================
